<%= render partial: "layouts/show_page", 
	locals: { 
		controller: :subjects,
		title: @subject.name,
		subtitle: @subject.species,
		images: @images,
		image_title_field: "location.name",
		image_subtitle_field: nil,
	}
%>
