<%= render partial: "layouts/object_card_mobile", 
	locals: { 
		link: "locations/#{location.id}",
		image_path: location.images.sample.thumbnail_path,
		title: location.name,
		subtitles: [],
		badges: location.subjects.order(:name),
		max_badges: 5,
		import: location.imports.order("date(imports.created_at) DESC").first,
	}
%>
