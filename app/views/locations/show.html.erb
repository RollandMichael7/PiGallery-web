<%= render partial: "layouts/show_page", 
	locals: { 
		controller: :locations,
		title: @location.name,
		subtitle: nil,
		images: @images,
		image_title_field: "subject.name",
		image_subtitle_field: nil,
	}
%>
