<%= render partial: "layouts/object_card", 
	locals: { 
		link: "locations/#{location.id}",
		image_path: location.images.sample.thumbnail_path,
		title: location.name,
		subtitles: [],
		badges: location.subjects.order(:name),
		max_badges: 5,
		import: location.imports.order(:created_at).first,
	}
%>
