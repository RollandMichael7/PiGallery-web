<%= render partial: "layouts/show_page", 
	locals: { 
		controller: :imports,
		title: "#{@import.created_at.strftime("%B #{@import.created_at.day.ordinalize} %Y")} Update",
		subtitle: nil,
		images: @images,
		image_title_field: "subject.name",
		image_subtitle_field: "location.name",
	}
%>
